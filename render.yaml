services:
  - type: web
    name: stocktrack-api
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: ALLOWED_ORIGIN
        value: https://stocktrack-vert.vercel.app
      - key: DATABASE_URL
        fromDatabase:
          name: stocktrack-db
          property: connectionString