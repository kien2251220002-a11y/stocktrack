version: '3.8'

services:
  app:
    build: .
    container_name: stocktrack-app
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - DB_PATH=/app/data/inventory.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped

volumes:
  data:
